import React from 'react';
import { Box } from '@mui/material';

// import Content_Apropos from '../../views/apropos/content_apropos';
import ContentTracker from '../../views/tracker/content_Tracker';

const Tracker: React.FC = () => {
    return (
        <Box>
            <ContentTracker />
        </Box>
    );  
};


export default Tracker;